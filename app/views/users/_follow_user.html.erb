<% @following_users.each do |following_user| %>
  <div class="row col-md-6 col-md-offset-3 topic-unit">
    <div class="pull-left col-md-6">
      <%= image_tag following_user.profile.image.url, class: 'img-responsive' %>
    </div>
    <div class="pull-left col-md-6">
      <div>
        <%= link_to users_path(id: following_user) do %>
            <%= following_user.name %>
        <% end %>
      </div>
      <div>
        投稿<%= following_user.topics.count %>件　フォロワー <span><%= following_user.follows.count %></span>　フォロー <span><%= Follow.where(follower_id: following_user).count %></span>
      </div>
    </div>
  </div>
<% end %>
